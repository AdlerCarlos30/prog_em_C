#include <stdio.h>

int main() {
    int contador = 0;
for(contador; contador <= 10; contador++)
{
printf("\n%d\n", contador);
}
for(contador; contador >= 0; contador--)
{
printf("\n%d\n", contador);
}

    return 0;
}
